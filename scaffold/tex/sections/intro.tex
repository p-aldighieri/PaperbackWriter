\section{Introduction}\label{sec:intro}

% Owned by the `intro-writer` agent. See STRUCTURE.md for the spec.
% Inputs: STRUCTURE.md + ../brief.md + ../proofs/ + ../tex/refs.bib
%
% The motivation block (typically P1-P2) can be re-tightened by the
% `motivation-writer` agent via the `motivation-pass` skill.

% TODO: drafted by intro-writer.
